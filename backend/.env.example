# Server
PORT=5000

# MongoDB
MONGO_URI=mongodb+srv://kaviyavelu2806_db_user:q0Tzw64lLLNp3SVE@cluster0.2nfc6dj.mongodb.net/bulkmail?retryWrites=true&w=majority

# JWT
JWT_SECRET=replace_this_with_a_long_random_secret
JWT_EXPIRES_IN=1d

# Admin login credentials (basic single-admin auth for this demo app)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123

# SMTP / Nodemailer
# Example values below work with Gmail if you create an "App Password"
# (Google Account -> Security -> 2-Step Verification -> App Passwords).
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password
MAIL_FROM_NAME=Bulk Mailer

# Frontend origin allowed for CORS
CLIENT_ORIGIN=http://localhost:5173
